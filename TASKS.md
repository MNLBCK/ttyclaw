# Completion tasks

- [x] Test the command against a real authenticated OpenClaw installation.
- [x] Keep repository clone plus `install.sh` as the distribution channel for now. Revisit Homebrew or npm only for a public release or users who should not manage a Git checkout.
- [ ] Add a CI smoke test with a mocked `openclaw` response before publishing releases.
- [ ] Tag the first release after the checks above pass.
